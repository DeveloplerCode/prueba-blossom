# prueba-blossom


instalar fuera de la terminal del contenedor para que lo copie el priyecto npm install sequelize-cli --save-dev

copia D:\tutoriales\prueba-blossom\config-database-migration el config.js en la carpeta que se crea despues de crear npx sequelize init crea una carpeta config copialo ahi 

<!-- sequelize migration:generate --name create-character-table -->
